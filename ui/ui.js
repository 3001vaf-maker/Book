const escapeMap = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' };
export const escapeHtml = (value = '') => String(value).replace(/[&<>"']/g, (char) => escapeMap[char]);
export function pageHeader(title, subtitle = '') { return `<header class="page-header"><h1>${escapeHtml(title)}</h1>${subtitle ? `<p>${escapeHtml(subtitle)}</p>` : ''}</header>`; }
export function button(label, { className = '', data = '', type = 'button', aria = '' } = {}) { return `<button type="${type}" class="ui-button ${className}" ${data}${aria ? ` aria-label="${escapeHtml(aria)}"` : ''}>${label}</button>`; }
export function iconButton(label, { className = '', data = '', aria = label } = {}) { return `<button type="button" class="icon-button ${className}" ${data} aria-label="${escapeHtml(aria)}">${label}</button>`; }
export function accordion(items, { openFirst = false } = {}) { return `<div class="accordion" data-accordion>${items.map((item, index) => `<section class="accordion-item ${openFirst && index === 0 ? 'is-open' : ''}"><button type="button" class="accordion-trigger" aria-expanded="${openFirst && index === 0}" data-accordion-trigger><span>${escapeHtml(item.title)}</span><span class="accordion-value">${item.value || ''}</span><span class="accordion-chevron">⌄</span></button><div class="accordion-panel" data-accordion-panel>${item.content}</div></section>`).join('')}</div>`; }
export function field({ label, name, value = '', type = 'text', placeholder = '', required = false, readonly = false, inputmode = '' }) { return `<label class="field"><span>${escapeHtml(label)}</span><input name="${escapeHtml(name)}" type="${type}" value="${escapeHtml(value)}" placeholder="${escapeHtml(placeholder)}" ${required ? 'required' : ''} ${readonly ? 'readonly' : ''} ${inputmode ? `inputmode="${inputmode}"` : ''}></label>`; }
export function bottomNavigation(active) { const items = [['main','Главная','⌂'],['timetable','График','◷'],['journal','Журнал','▤'],['chat','Чат','◌'],['settings','Настройки','⚙']]; return `<nav class="bottom-nav" aria-label="Основная навигация">${items.map(([id,label,icon]) => `<button type="button" class="nav-item ${active === id ? 'is-active' : ''}" data-nav="${id}"><span class="nav-icon" aria-hidden="true">${icon}</span><span>${label}</span></button>`).join('')}</nav>`; }
export function emptyState(title, text = '') { return `<div class="empty-state"><strong>${escapeHtml(title)}</strong>${text ? `<span>${escapeHtml(text)}</span>` : ''}</div>`; }
export function initAccordions(root) { root.querySelectorAll('[data-accordion]').forEach((accordionRoot) => accordionRoot.addEventListener('click', (event) => { const trigger = event.target.closest('[data-accordion-trigger]'); if (!trigger) return; const item = trigger.closest('.accordion-item'); const open = item.classList.toggle('is-open'); trigger.setAttribute('aria-expanded', String(open)); })); }
export function modal(content, { title = '' } = {}) { return `<div class="modal-backdrop" data-modal><div class="modal-sheet" role="dialog" aria-modal="true" ${title ? `aria-label="${escapeHtml(title)}"` : ''}><button type="button" class="modal-close" data-modal-close aria-label="Закрыть">×</button>${content}</div></div>`; }
export function mountModal(root, html) { root.insertAdjacentHTML('beforeend', html); const modalRoot = root.querySelector('[data-modal]:last-of-type'); modalRoot?.addEventListener('click', (event) => { if (event.target.matches('[data-modal], [data-modal-close]')) modalRoot.remove(); }); return modalRoot; }
